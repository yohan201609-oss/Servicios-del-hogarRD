name: Flutter Web Legacy

on:
  workflow_dispatch:

jobs:
  web:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: 🐦 Setup Flutter Legacy
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
        cache: true
        
    - name: 📦 Get Dependencies
      run: flutter pub get
      
    - name: 🔍 Analyze Code
      run: flutter analyze --no-fatal-infos
      continue-on-error: true
      
    - name: 🏗️ Build Web (Legacy)
      run: |
        echo "Building web version with legacy Flutter..."
        flutter build web --web-renderer html --no-tree-shake-icons
      
    - name: 📱 Upload Web Build
      uses: actions/upload-artifact@v4
      with:
        name: web-legacy
        path: build/web/
        retention-days: 30
        
    - name: 📊 Web Build Summary
      run: |
        echo "## 🌐 Web Build Legacy Completado!" >> $GITHUB_STEP_SUMMARY
        echo "- **Flutter Version:** 3.24.0" >> $GITHUB_STEP_SUMMARY
        echo "- **Build:** Web version ready" >> $GITHUB_STEP_SUMMARY
        echo "- **Files:** Available in Artifacts" >> $GITHUB_STEP_SUMMARY
